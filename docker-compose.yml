version: "3.8"

services:
  backend:
    build: ./app
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend


      ## per docker: http://backend:5000 anziché localhost